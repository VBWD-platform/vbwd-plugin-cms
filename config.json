{
  "uploads_base_path": {
    "type": "string",
    "default": "/app/uploads",
    "description": "Filesystem path where uploaded images are stored"
  },
  "uploads_base_url": {
    "type": "string",
    "default": "/uploads",
    "description": "Public URL prefix for serving uploaded images"
  },
  "max_file_size_bytes": {
    "type": "number",
    "default": 10485760,
    "description": "Maximum allowed upload file size in bytes (default: 10 MB)"
  },
  "allowed_mime_types": {
    "type": "string",
    "default": "image/jpeg,image/png,image/webp,image/gif,image/svg+xml",
    "description": "Comma-separated list of allowed image MIME types"
  },
  "default_robots": {
    "type": "string",
    "default": "index,follow",
    "description": "Default robots meta value applied to new pages"
  },
  "default_language": {
    "type": "string",
    "default": "en",
    "description": "Default language code applied to new pages"
  }
}
